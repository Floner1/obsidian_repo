Bro i wish school would come earlier so i can get my fucking ass away from dad bro

tips:
- search for wrong answer choices, nto correct ones
- look for ones that are too broad and not specific enough to text and eliminate
- opposite trap: says reverse to what author believes
- true but irrelevant: contains details in the passage but details dont answer question
- read question first, not passage
- filter out passage:
	- ignore names
	- scientific phrases/industry specific phrases
	- overly specific examples
	- skip to what question wants
- visualize the passage in your head, if u can visualize, u are able to comprehend and understand text
- predict answers, stops brain from falling for trap answers, proves u understand text fully enough
- eliminate wrong answers

Peter,

- Built pipeline error-states + analytics for SaiPrice, branch pipeline-error-states-and-analytics
    
- Design spec + implementation plan written and committed first: 9 scraper failure codes, 3 scoring failure codes, new ScoringRun table, median APE as the accuracy metric, labeled in-sample
    
- Executed inline, task by task, each gated by a failing test then a passing test then a commit
    
- client.fetch() now returns (response, error_code) instead of a bare None, distinguishes bot walls from timeouts from dead URLs
    
- New soft-gone placeholder detection for alonhadat's dead-LDP shell page
    
- ScrapeRun.status_counts + new ScoringRun table added via migration, applied to live DB
    
- LOGGING configured in settings.py, INFO logs were previously discarded silently
    
- New /health/ dashboard: scrapes-per-day (30 days) and model accuracy trend, server-rendered, no JS, no new dependencies
    
- Two real bugs found and fixed outside original scope: run counters lost on mid-loop exceptions, hard-kill runs left no recoverable state
    
- Code review caught one Critical bug: a caught model failure was nulling predicted_price on every active listing, not just the affected rows. Verified independently, fixed, three regression tests added
    
- Three Important fixes from review: aborted runs no longer stamp finished_at (was corrupting the delisting-sweep baseline), sweep_delistings now refuses to run on zero listings_seen (was capable of mass-delisting after two walled runs back to back), status labels now distinguish "blocked" from "empty" instead of rendering identically
    
- CLAUDE.md updated to match the shipped code (§5.4, §7, §8, §9, §11)
    
- Test count: 213 to 290 (+77)
    
- Verified against live DB: real score_listings run written, median_ape=0.1538 in-sample vs the model's 22.87% held-out, confirms the honesty-labeling was worth building
    
- Verified against live dev server: /health/ renders real data, aborted/blocked/empty/ok labels all correct
    
- Live scrape against alonhadat deliberately skipped, deferred to tomorrow's 9 AM scheduled run to avoid a manual wall-risk fire
    
- Merge to master staged with --no-commit --no-ff, nothing committed, message and commit left for you
    
- Open for tomorrow: check ScrapeRun 23's status_counts once the 9 AM run lands, first real test of the new fetch/taxonomy code against live alonhadat HTML

to do list: 
check out all mobility exercises and ask claude whihc one to add

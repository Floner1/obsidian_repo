here is what was done to hotel:
**Scope**  
Repo: Floner1/hotel-app-test, branch harden/chat-widget merged into feat/local-llm-chat-widget. Files touched: ai_providers.py, services.py, views.py, chat-widget.js, test_chat.py, plus new test_chat_hardening.py.

**Security findings identified**

- ChatML control-token injection (critical) — guest message containing `<|im_end|><|im_start|>system` forged a real system turn. Reproduced live: extracted a planted secret verbatim.
- DB content (room/service descriptions, hotel name) interpolated raw into system prompt with no delimiting between data and instructions.
- No timeout on Ollama client calls (defaulted to None, could hang a Django worker indefinitely).
- strip_thinking failed open on truncated output — could leak raw model reasoning to a guest.
- User message concatenated with control marker with no sanitization.
- Rate limiting and 429 handling: initially misdiagnosed as missing, turned out to already exist (10/m per-IP, correct 429 status). Real gaps were missing Retry-After header and no per-session limit.

**Security fixes shipped**

- sanitize_prompt_text() added — strips ChatML tokens, think tags, tool-call tags via iterative regex pass. Runs inside the provider so no caller can skip it.
- Applied to both guest input and all DB-sourced fields feeding the system prompt.
- System prompt now fences DB content between BEGIN/END HOTEL DATA markers, with rules kept outside the fence.
- Request timeout added to Ollama client (initially 60s, later re-derived, see below).
- strip_thinking rewritten to fail closed — an unterminated reasoning block now returns empty rather than leaking the monologue.
- Per-session rate limit added (via request.session, using .create() not .save() after a bug where .save() didn't set the session cookie) alongside existing per-IP limit.
- 429 response now carries a Retry-After header; widget JS reads it and shows guest a wait time instead of a generic error.
- Rate limits set at 15/min per session, 20/min per IP initially, later changed to 15/min session, 35/min IP per your instruction.
- Output escaping (jQuery .text(), no innerHTML) confirmed already safe, locked with a regression test.
- Confirmed model is never given tool access — text in, text out only, no path to trigger bookings or DB writes.

**Speed work — investigated, no durable win found**

- Profiled latency: ~98% of wall time is token generation, dominated by the model's compulsive reasoning (qwen3:4b's chat template unconditionally opens a `<think>` block).
- Tested think=False, think='low', assistant-message prefill, and raw pre-closed think blocks — none stop the model from reasoning. Root cause is architectural, not a prompt-level fix.
- keep_alive='30m' added — removes a measured 4.95s cold-start reload after Ollama idles out. This is the one real, verified speed improvement shipped.
- think=True set explicitly — required to keep reasoning output separated from the answer instead of leaking into it; not a speed change.
- Tested moving the /no_think marker from both messages to user-only. Initial 6-sample test showed 2x improvement. Re-tested properly at n=24 paired samples: result reversed (median said faster, mean said slower, one question regressed with 4/8 empty retries). Change was reverted — no real effect, one real harm.
- Conclusion: warm-path latency is unchanged from before this session. The only durable fix for perceived speed is response streaming, which was scoped but explicitly not built this session (requires redesigning the retry loop and strip_thinking to work on partial output, not a simple flag flip).

**Live bug found and fixed (post-merge)**

- Symptom: "assistant unavailable" on an ordinary message.
- Root cause: reasoning and answer share one token budget (1200); the model's reasoning sometimes consumes the entire budget, leaving no answer. The retry loop re-sent the identical request with the identical budget and failed the same way twice.
- Fix: retry now escalates to a larger budget (2400 tokens) instead of repeating.
- Follow-on bug caught by live testing (not by the test suite): the 60s timeout was shorter than the new 2400-token retry needed, causing read timeouts on long replies. Timeout is now derived from the retry budget and a measured tokens/sec floor, rather than a hand-picked number.
- Verified live: 42.9s round trip, correct grounded answer, first attempt exhausted at 1200 tokens, retry succeeded at 2400.
- Known limitation, not fixed: reasoning length is still unbounded (observed 1,500 to 9,200+ characters on the same question across runs). This mitigates the empty-reply failure, it doesn't eliminate it.

**Process notes**

- Work done in an isolated git worktree (harden/chat-widget) off the feature branch, per your standing workflow.
- Full test suite: 105 passing before this work, 130 passing after (23 new hardening tests), run multiple times for stability.
- Two of Claude's own claims were made and then retracted after re-measurement: an initial "no rate limiting exists" finding (wrong, it existed) and an initial "2x speedup" claim from the marker change (didn't hold at proper sample size).
- Merge conflict occurred because you had independently committed the indentation fix mid-session on the main branch; resolved in favor of the hardened branch's version. Verified via diff that nothing was lost in the process.
- Both commits (feature commit 9bde1c1, merge commit 8afc5c0) checked clean of any AI-tool attribution.

**Still open, not addressed this session**

- think=True is now sent unconditionally to Ollama. If AI_MODEL is ever changed to a non-thinking model, this will break chat with an HTTP 400. Flagged, not fixed.
- No concurrency cap in front of Ollama — it serves one request at a time, so the IP rate limit (35/min) is well above what the machine can actually process; rate limits here are abuse control, not capacity protection.
- Streaming (the actual fix for perceived response speed) is scoped but not started.

SAT ENGLISH:
- had infront of verb only when that verb is the second event in that sentence, eg, she arrived late to the place, the meeting had finished. 
- had/have/has only appears in main clause, if verb is modifying (eg has ing or ed), then no has/had/have. had/has/have is called auxiliary. have and has only need 1 event, past action with present relevance 
- **finite verbs**: ability to show tense and agree with subject
- **main verb**: carries primary meaning/action in sentence, regardless of tense/agreement
- tests for variances of verbs:
	- relative pronouns:
		- is there a "who","which", "that" or similar right before the blank? if yes then it is a finite verb, agree with whatever object is in sentence. plural = no s, single = s
	- 